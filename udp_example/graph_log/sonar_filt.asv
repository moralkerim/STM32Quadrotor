function [sonar_filt] = sonar_filt(sonar)
    sonar_filt = zeros(arr_size);
    arr_size = length(sonar);
    for i=2:arr_size
        if(sonar(i) > 5)
            sonar(i) = sonar(i-1);
        end
    end
end

